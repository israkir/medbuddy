# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **`lookup_health_history` agent tool:** OpenAI function schema plus orchestrator dispatch; returns recent health-issue events (optional `since_days` window, cap 30) via `LookupHealthHistoryTool`, with `health_history.empty` when none match.

### Changed

- **Patient LLM context:** Last 12 health-issue events are summarized into the persona block (`recent_health_notes_header`); `preferred_name` is surfaced as an explicit `prompts.address_user_as` directive and the medication agent system prompt treats it as a hard instruction for greetings and direct address.
- **Reminder horizon → chronic/indefinite:** Pending replies like “every day”, “long-term”, 長期, 終身, etc. clear horizon pending, merge reminder metadata (`needs_horizon_confirmation=false`, `materialize_daily`, `horizon_days` cleared), optionally set `is_indefinite` via `merge_medication_raw_metadata(..., set_indefinite=)` (Supabase + mock), run `sync_and_enqueue_reminders`, and reply with `reminder.horizon_confirmed_indefinite`.
- **Medication add confirmation:** After confirm, `persist_medication_add_from_draft` passes `suppress_horizon_question` so the post-add LLM reply does not duplicate the “how many days?” appendix while `_maybe_append_pending_reminder` still nudges on the next turn; `suppress_horizon_appendix` threaded through `LLMPort`, Gemini/OpenAI/mocks, and `reminder_compose_appendix`.
- **Emergency demo copy (en + zh-TW):** Urgent 119-first wording with embedded `reason` / `contact_hint` in `emergency_with_saved_contact`; shorter `simulated_emergency_notify`; medication agent uses the combined template; API tests updated for the new phrases.

- **Emergency contacts in LLM context:** Multi-contact rows are described with bracketed tokens (`[EMERGENCY_CONTACT_N]`) plus relationship and channel type; real values live in a per-turn map and the orchestrator substitutes them into the model's final text before the patient sees it (unmapped tokens stripped with a warning). `format_patient_profile_signals_for_llm` returns `(signals, token_map)`; the orchestrator refreshes the system prompt after medication CRUD tool batches as well as profile / health tools. New locale strings for the redacted block; tests cover `emergency_contacts_redacted_hint`.
- **Reminder horizon replies:** Pending expiry returns `reminder.horizon_expired_ack` instead of a silent clear; `_parse_horizon_days` accepts word-form day counts (e.g. "seven days", 七天), trailing integers after short filler ("yes 7"), and related patterns.
- **LINE webhook and voice UX:** `POST /v1/line/webhook` returns immediately after parse and idempotency, scheduling each event on FastAPI `BackgroundTasks` (failures logged in `_run_event`). Inbound **text and audio** start LINE's chat loading indicator before the assistant path (text: before LLM; audio: before download/STT). New `LineMessagingPort.send_chat_loading_indicator` on `LineHttpClient` and `MockLineClient.loading_indicators`; tests cover both paths.

### Docs

- **Documentation alignment pass:** Updated `docs/features.md`, `docs/tdd-extended.md`, `docs/llm-context.md`, and `docs/privacy.md` to replace the removed `patients.health_notes` column with the `patient_health_conditions` structured table; distinguished the live `include_health_notes` / `recent_health_notes` code identifiers from the removed DB column; added `is_indefinite` / chronic-med notes where missing. Updated `docs/presentation/index.html`: added `lookup_health_history` tool and chat loading indicators to the built-features checklist; expanded capability cards with health-history lookup and multiple emergency contacts and set the patient-capabilities headline to nineteen total; dropped Anthropic/Claude pricing and prompt-caching citations (only Gemini + OpenAI are integrated providers). Infrastructure-evolution slide: removed two redundant callouts after the cost table.
- **Docs vs code (post-add + ops):** Aligned post-add medication flow across `docs/index.md`, `features.md`, `use-cases.md`, `tdd.md`, `tdd-extended.md`, `privacy.md`, and `llm-context.md` — **`post_add_interaction_crosscheck`** / **`compose_medication_added_primary`** (not chat **`check_interactions_structured`**). Extended `llm-context.md` with missing `LLMPort` methods. `apps/backend/README.md`: purge endpoint, env vars, `health_conditions` on `/v1/app/me`. `features.md`: LINE async webhook, idempotency, loading indicators, duplicate-med guard, orchestrator tools. `frontend-expo.md` / `apps/frontend/README.md`: onboarding → `health_conditions`.
- **Project Q&A (`docs/qna.md`):** New engineer-facing Q&A (Q1–Q55): end-to-end pipeline walkthrough with redaction I/O, early-exit gates, memory layers, reminders/chronic/reconcile, drug personalization cache (hashing + examples), voice/LINE/adherence/health-summary topics, and Mermaid flow/component diagrams. Root `README.md` and `docs/index.md` link the doc; `docs/presentation/index.html` and cross-doc references updated where needed.
- **Deck “What's already built” checklist completed:** Added the three orchestrator tools that were missing from the short-label list — `list_upcoming_doses` (“upcoming doses”), `manage_health_conditions` (“manage health conditions”), and `simulate_notify_emergency_contact` (“simulated emergency notify”). The slide now enumerates all 19 tools registered in `llm/agent_tool_definitions.py`, matching `docs/qna.md` Q17.
- **Deck documents overview:** Added `qna.md` row (Q1–Q55 — pipeline, tools, reminders, privacy, design tradeoffs) to the “How to read this deck” documents table.

### Security

- **LINE webhook idempotency (S2):** A Redis `SETNX` keyed on `webhookEventId` (5-minute TTL) deduplicates events before dispatch. LINE retries and slow-200 re-deliveries are silently skipped. Falls back gracefully (always-process) when `REDIS_URL` is unset so mock mode is unaffected. New module: `apps/backend/src/medbuddy/channels/line/idempotency.py`.

- **Supabase RLS tightened:** All MedBuddy application tables (including `patient_health_conditions`) deny access to the `anon` and `authenticated` roles. The backend uses `SUPABASE_SERVICE_KEY` (Supabase **Secret** API key, `sb_secret_...` only), which bypasses RLS without requiring explicit policies. Legacy open `using (true)` policies are dropped in `apps/backend/supabase/schema.sql` (and `recreate_database.sql`).
- **Supabase service-key fail-closed:** `create_supabase_client` raises `ConfigError` if `SUPABASE_SERVICE_KEY` is absent or not an `sb_secret_...` value (including publishable keys and legacy JWT secrets), avoiding PostgREST `42501 permission denied for table patients` from misconfigured env.
- **Constant-time cron secret comparison:** `POST /internal/reminders/reconcile` now uses `secrets.compare_digest` for the `X-Cron-Secret` header check, matching the pattern already used for `MEDBUDDY_MOBILE_BEARER_TOKEN`.

### Fixed

- **Medication agent pending footers:** Templated add-confirmation and reminder-horizon reminders append only when the corresponding pending row already existed before the orchestrator turn, avoiding a duplicate question when the model just created the pending in the same reply.
- **Profile nudge after in-turn saves:** Reload `user_row` after `run_tool_agent_loop` so `append_profile_completion_nudge_if_due` does not fire on fields that were saved during the tool loop.
- **Health conditions persist end-to-end:** Allergies and chronic conditions are stored only in `patient_health_conditions` (no `patients.health_notes`). The agent uses `manage_health_conditions` with `action` / `conditions` / `condition_id`; profile extraction no longer carries free-text health fields. Post-add medication runs `check_drug_condition_interactions`; health summary and journal export include structured conditions. Profile completion nudge and persona gaps use active condition count; `orchestrator_profile_saved_hint_fields` limits acknowledgements to keys that were actually saved.
- **Supabase DDL parity and `service_role` grants:** `revoke all … from anon, authenticated`, `grant usage on schema public`, and per-table `grant all … to service_role` (including `conversation_turns_id_seq`) live in `apps/backend/supabase/schema.sql` and are mirrored in `recreate_database.sql` for a one-file destructive reset.
- **ARQ reminder worker startup:** Worker `on_startup` now creates a shared `httpx.AsyncClient` (matching `main.py` timeouts/limits) and passes it to `build_app_services`, then closes it in `on_shutdown`. Production mode previously required `outbound_http` from the FastAPI lifespan only, so the worker crashed with `ConfigError` before processing jobs.
- **Drug-registry parallel fetches:** TFDA and OpenFDA requests in `fetch_drug_grounding_text`, `ExplainMedicationTool`, and `ListMedicationsTool` now run concurrently via `asyncio.gather` instead of sequentially, cutting ~200–400 ms per tool call. Per-medication groundings in `list_medications` are also gathered in parallel.
- **Drug-grounding helper deduplicated:** `side_effects._fetch_grounding` was an inline copy of `drug_grounding.fetch_drug_grounding_text`; it now delegates to the canonical helper, removing ~15 lines of duplicate logic.
- **Drug-registry timeouts and retry:** `HttpDrugData` uses a structured `httpx.Timeout(connect=3, read=4, write=3, pool=3)` and retries once with random jitter on `TimeoutException` / `ConnectError`. Dropped the unused `timeout: float` constructor parameter.
- **`outbound_http` single-owner:** `build_app_services` now raises `ConfigError` in production mode when `outbound_http` is `None`, eliminating the silent fallback that could construct a second untracked `AsyncClient`. The lifespan in `main.py` is the sole owner.
- **`make_mock_settings` deduplicated:** `tests/conftest.py` previously duplicated the helper from `tests/helpers.py`; it now imports from the canonical location.
- **Reminder job `scheduled_at` version check (R3):** `send_reminder_for_dose` now receives the `scheduled_at` ISO timestamp baked in at enqueue time. If the live row's `scheduled_at` no longer matches (row rescheduled or recreated), the job is dropped with an INFO log instead of double-firing.
- **Nudge fan-out jitter (R4):** Nudge `defer_until` offsets receive ±30 s random jitter at enqueue time, spreading concurrent pushes across the minute boundary.
- **Emergency-contact atomic set-primary (R5):** The two-step demote-then-promote UPDATE sequence is replaced by a single Postgres function call (`medbuddy_set_emergency_primary`), eliminating the race window where a concurrent insert could leave multiple `is_primary = true` rows. Defined in `apps/backend/supabase/schema.sql` and `recreate_database.sql`.
- **`MEDBUDDY_REMINDER_DEFAULT_LOCAL_TIME` format validated (O5):** `load_settings` now rejects values that are not zero-padded `HH:MM` (e.g. `"9:0"`, `"25:00"`) at startup with a `ConfigError`.
- **`PUBLIC_BASE_URL` HTTPS enforced for voice replies (O6):** In production mode, if `MEDBUDDY_LINE_VOICE_REPLIES` is not `"off"`, startup raises `ConfigError` when `PUBLIC_BASE_URL` is not `https://…`. LINE's CDN requires HTTPS for audio content URLs.
- **PHI persona body gated to DEBUG (S3):** `build_patient_context_for_llm` now logs `chars=N` at INFO and the full body only at DEBUG — patient names, meds, and demographics no longer appear in production INFO logs. `PhiRedactFilter` added to all named loggers via `core/logging.py`; it masks emails, phone numbers, and known PHI field names from structured log record extras.
- **Pending JSONB `schema_version` field (R6):** `DoseClarificationPending`, `ReminderHorizonPending`, and `MedicationAddConfirmationPending` now write `schema_version: 1` in `to_json()`. `from_json()` accepts version-0 rows (pre-migration, backward-compatible) and discards any future version it does not understand with a WARNING log.
- **`request_id` propagated into arq jobs (F5):** `enqueue_reminder_jobs` captures the current `request_id` contextvar and passes it as a job arg. `send_reminder_for_dose` restores it at task entry so reminder-delivery log lines are linkable to the web-turn that scheduled them. `_request_id` contextvar extracted to `core/request_id.py`.
- **Conversation history retention endpoint (P6):** `POST /internal/conversations/purge` (same `X-Cron-Secret` auth as reconcile) deletes `conversation_turns` older than `MEDBUDDY_CONVERSATION_RETENTION_DAYS` (default 90). `ConversationStorePort.purge_turns_older_than` added; `InMemoryConversationStore` and `SupabaseConversationStore` implement it. The `(patient_id, created_at)` index required for the range scan was already present in `schema.sql`.

### Added

- **`add_medication` duplicate guard:** Case-insensitive name match against existing medications returns `medication.already_on_file` (en + zh-TW) instead of starting a second draft.
- **Chronic / indefinite-duration medications:** New `medications.is_indefinite boolean not null default false` column (partial index `medications_is_indefinite_idx`) marks lifelong meds (hypertension, statins, thyroid replacement, …). `MedicationExtraction.is_indefinite` recognises chronic phrasing in en + zh-TW (e.g. "long-term", "every day from now on", "長期", "終身", "慢性病用藥"); when set the draft builder forces `needs_horizon_confirmation=false` and `materialize_daily_reminders=true`, so the "how many days?" follow-up is suppressed. The rolling-window materializer is unchanged — the window is refilled by two new mechanisms: a daily arq cron (`resync_chronic_meds_cron` in `medbuddy.reminders.worker`, default 03:15 UTC, configurable via `MEDBUDDY_CHRONIC_RESYNC_CRON_HOUR_UTC` / `MEDBUDDY_CHRONIC_RESYNC_CRON_MINUTE_UTC`) and a delivery-time safety net in `deliver_dose_reminder` that calls `sync_and_enqueue_reminders` when fewer than `MEDBUDDY_CHRONIC_DELIVERY_TOPUP_THRESHOLD` (default 3) future doses remain for the just-fired indefinite med. New `UserDataPort.list_patients_with_indefinite_medications` and `count_future_dose_events`; `DoseEventReminderPayload` now carries `medication_id` and `medication_is_indefinite`; new locale key `llm.added_indefinite` replaces the day-count copy when `is_indefinite=true`.
- **`patient_health_conditions`:** New Postgres table (unique on `patient_id`, `lower(name)`, `category`), `HealthConditionRecord` / `HealthConditionInput`, `UserDataPort.list_health_conditions`, `upsert_health_conditions`, `deactivate_health_condition`, LLM `extract_health_conditions`, and `check_drug_condition_interactions`. Orchestrator tool **`manage_health_conditions`**; patient context for tools includes a structured health block from saved rows.

### Removed

- **`patients.health_notes`:** Column and API field removed; standalone onboarding and `GET /v1/app/me` use `health_conditions` arrays instead. Expo onboarding submits structured rows derived from the notes field where applicable.

### Tests

- **Test tree mirrors `src/medbuddy/`:** Package tests live under `apps/backend/tests/medbuddy/<same path as medbuddy/>` (`application/` with `profile/`, `pending/`, `health_events/`, plus cross-cutting flows at `application/` root; `agents/tools/` includes `test_update_medication_tool.py` alongside other tool tests). `apps/backend/tests/conftest.py` and `apps/backend/tests/helpers.py` remain at `apps/backend/tests/` root.
- **ARQ worker lifecycle:** `tests/medbuddy/reminders/test_worker_lifecycle.py` covers mock startup (no shared HTTP client) and production-path startup/shutdown (shared `httpx.AsyncClient` closed). `tests/conftest.py` sets `REDIS_URL` via `setdefault` so importing `medbuddy.reminders.worker` (which resolves `WorkerSettings.redis_settings` at import time) does not require a live Redis in the environment.
- **Locale-aware LINE follow tests (O3):** Added `test_follow_seeds_zhtw_locale_from_line_profile_language`, `test_follow_unknown_language_falls_back_to_default_locale`, and `test_follow_no_profile_language_uses_stored_locale` to cover the locale-seeding logic shipped on this branch.
- **Pending JSONB version tests (R6):** `tests/medbuddy/core/test_pending_schema_version.py` covers version-0 backward compat, roundtrip, and future-version rejection for all three pending types.
- **Reminder version-check and conversation-purge tests (R3/P6):** `tests/medbuddy/reminders/test_reminder_version_jitter.py` covers `scheduled_at` mismatch skip, happy-path match, and the `/internal/conversations/purge` endpoint.
- **CI coverage gate (O4):** `pytest` in CI now runs with `--cov=medbuddy --cov-fail-under=65`; build fails if overall coverage drops below 65 %.

### Documentation

- **Backend tree and tests:** `apps/backend/README.md` package table and diagram use `src/medbuddy/…` consistently; testing section references `tests/medbuddy/…`. `docs/tdd-extended.md` §3 component map updated (e.g. `idempotency`, `post_add_medication_reply`, `drug_grounding*`, `emergency_contacts`, `request_id`, LLM tool-definition modules, `privacy/log_filter`, internal purge route). Cross-links and path shorthand fixed in `docs/tdd.md`, `docs/index.md`, `docs/features.md`, `docs/privacy.md`, `docs/reminders.md`, `docs/llm-context.md`, `docs/go-port-mapping.md`, and `TODO.md` architecture checklist.
- **Chronic / indefinite-duration medication docs rollout:** `docs/reminders.md` gains a dedicated *Chronic / indefinite-duration medications* section (daily `resync_chronic_meds_cron` + delivery-time top-up, both paths reuse `sync_and_enqueue_reminders`), an `is_indefinite` schema row, the three new `MEDBUDDY_CHRONIC_*` env vars, code-map updates (`chronic_resync.py`, persistence methods, top-up in `deliver.py`), and test references. `docs/features.md` §3.1 / §4.3 / §6 / §8 explain the LLM `is_indefinite` extraction, the suppressed horizon prompt, the `llm.added_indefinite` reply, and the new capability row in the reminders table. `docs/tdd-extended.md` §3 reminders tree adds `chronic_resync.py`, §4.3 covers both refill paths, §5.1 step 4 notes chronic meds never set `ReminderHorizonPending`, §6 medications schema documents `is_indefinite` + partial index, §9 annotates `MedicationExtraction.is_indefinite`, §15.6 adds the new env vars, and §18.2 clarifies the column lives outside `raw_metadata`. `docs/use-cases.md` §2 early exits and §3.3 add_medication now describe the chronic skip + bilingual example utterances; `docs/prd.md` *Build the list* paragraph notes chronic phrasing is auto-detected; `docs/index.md` gets a quick-lookup row; `docs/go-port-mapping.md` reminders mapping table adds `chronic_resync.py`; `apps/backend/README.md` LINE-reminders section summarises the chronic refill paths and env vars; and the deck (`docs/presentation/index.html`) surfaces the feature on the *What's already built*, *Reminder UX*, and *Reminders flow* slides.

### Added

- **Fail-closed startup validation:** When `MEDBUDDY_INTEGRATION=production`, the app raises `ConfigError` at startup if any required secret is absent (`MEDBUDDY_MOBILE_BEARER_TOKEN`, `LINE_CHANNEL_SECRET`, `LINE_CHANNEL_ACCESS_TOKEN`, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `MEDBUDDY_CRON_SECRET`, and the active LLM API key). The container exits immediately instead of booting to a degraded 503-per-request state.
- **Global exception handler + request ID:** All unhandled exceptions return `{"code": "internal_error", "request_id": "<uuid>"}` with HTTP 500 — no raw tracebacks leak to clients. A `RequestIdMiddleware` stamps each request with a UUID available in logs and the `X-Request-Id` response header.
- **CORS middleware:** `CORSMiddleware` is active when `CORS_ALLOWED_ORIGINS` is set (comma-separated list). Applies to `GET`/`POST` with `Authorization`, `Content-Type`, `X-App-User-Id`, and `X-MedBuddy-Locale` headers. Defaults to off (empty list).
- **Hardened Docker runtime:** `docker-entrypoint-web.sh` now passes `--proxy-headers`, `--forwarded-allow-ips='*'`, `--workers ${WEB_CONCURRENCY:-2}`, and `--timeout-graceful-shutdown 25` to uvicorn. `Dockerfile` adds a non-root `medbuddy` system user and a `HEALTHCHECK` against `GET /health`.

### Added

- **Supabase row timestamps:** `patients`, `medications`, `dose_events`, and `drug_reference_cache` now have `created_at` / `updated_at` (`fetched_at` on reference cache unchanged). `medbuddy_touch_updated_at()` plus `BEFORE UPDATE` triggers keep `updated_at` current on those tables and on `emergency_contacts` / `drug_personalization_cache`. Append-only `conversation_turns` and `health_issue_events` stay `created_at`-only. `apps/backend/supabase/schema.sql` tail adds columns for existing installs and backfills reference-cache timestamps from `fetched_at`.

- **Multiple emergency contacts with auto-primary on most recent:** Each patient may keep any number of emergency contacts on file. Saves are now **additive** — `save_onboarding_profile` and `patch_user_profile` upsert by `(channel_type, channel_value)` instead of wiping every row, so older contacts persist when a newer one is added. After every write, `is_primary` is forced **`true`** on the row with the latest `updated_at` and **`false`** on every other row — the most recently saved contact is always the single primary, regardless of any client-supplied flag. On the **`Intent.EMERGENCY`** branch and the **`simulate_notify_emergency_contact`** tool, the simulated outreach line now lists **every** stored contact via the new **`emergency_contacts_hint_all`** helper (locale-aware separator), so users see that all contacts would be alerted in a real outage. New unit + integration tests cover merge semantics, demote-old-primary writes, and multi-contact notify copy.
- **Post-add interaction scan:** When saving a new medication and the patient already has at least one other drug on file, the backend appends a structured cross-check to the post-add reply (`medication.post_add_interaction_*` locale strings), using full list plus grounding for the new drug. The continuation focused **`post_add_interaction_crosscheck`** prompt is separate from chat **`interaction_check`** (`check_interactions_structured`). First-ever medication on the list is unchanged (no extra interaction LLM call).
- **Locale defaults:** LINE **follow** welcome and assistant replies seed `patients.locale` from LINE’s user profile **`language`** (with httpx `GET /v2/bot/profile/{userId}`) when it maps to `en` or `zh-TW`. Standalone **`GET /v1/app/me`** applies **`X-MedBuddy-Locale`** (device tag from Expo) or the **first** `Accept-Language` entry to update stored locale **only until** onboarding is completed, so server-side copy matches the device before the user submits onboarding.

### Fixed

- **Agent profile context after `update_profile` tool:** Within one assistant turn, the medication tool loop now reloads the patient row after `update_profile` persists (e.g. `preferred_name`) and rebuilds the system message so later LLM hops see updated “Patient / schedule context” instead of stale gaps. Regression test in `tests/medbuddy/application/test_orchestrator_conversation_history.py` (spy snapshots messages with `copy.deepcopy`). Agent system prompt nudges occasional use of the stored form of address when present.
- **Dose reminders (arq):** Building LLM patient context no longer runs `sync_upcoming_dose_events` by default, so chat turns do not delete/recreate future `dose_events` rows and orphan deferred `send_reminder_for_dose` jobs in Redis. `ListUpcomingDosesTool` uses `sync_and_enqueue_reminders` so explicitly listing upcoming doses refreshes jobs. Stale-id skips are logged at INFO.
- **Voice/text assistant persistence:** `Intent.UPDATE_PROFILE` now applies `extract_profile_patch` + `patch_user_profile` before the tool loop, and the planner is given a filtered tool list (no `update_profile`) plus a system hint when a patch was saved — so demographics from speech or chat persist even when the chat model returns prose only. Classifier adherence slots (`record_pending_dose_as_taken`, `dose_adherence_note`) are merged into `confirm_dose` execution; when the planner calls `report_side_effects` without `confirm_dose` but the classifier recorded dose + note (e.g. took pill and feels dizzy), the server runs a mechanical `confirm_dose` pass so dose rows stay accurate.
- **Drug reference cache:** Follow-ups like “sure” / 「好」 are no longer passed verbatim to OpenFDA or stored under junk `drug_reference_cache.query_key` values. `explain_medication` and `interaction_check` accept optional `drug_query` / `medication_id`; the server resolves weak user text using recent assistant turns and catalog names (plus a small vitamin C fallback when the drug is only mentioned in chat). `add_medication` draft extraction receives a short **redacted** recent-thread excerpt for the same reason, so confirmations after an add/reminder offer still ground drug name and timing. `CachingDrugData` also refuses weak keys so stray callers cannot populate the shared cache with stopwords.

### Changed

- **Supabase canonical DDL:** Removed `apps/backend/supabase/migrations/` standalone SQL (`restrict_rls_to_service_role.sql`, `set_emergency_primary_fn.sql`, folder README). `medbuddy_set_emergency_primary` and `grant execute … to service_role` are defined in `apps/backend/supabase/schema.sql` and `recreate_database.sql`; changelog, backend README, TDD extended, and TODO now reference those paths for RLS and drift.

- **Integration env surface:** `MEDBUDDY_INTEGRATION` is only `mock`, `production`, or unset (defaults to `mock`). Removed `MOCK_EXTERNAL_SERVICES`, `local` / `dev` / `default` aliases, and docs claiming `RENDER`-based overrides. `SUPABASE_SERVICE_KEY` must be the Supabase Secret key (`sb_secret_...`); `SUPABASE_PUBLISHABLE_KEY` was removed from `Settings`. CI, Makefile, `compose.yaml`, and `render.yaml` updated accordingly.

- **Integration mode naming:** “Real mode” is now **production mode** in user-facing errors and docs. `IntegrationMode.REAL` is `IntegrationMode.PRODUCTION` with value `production`; set `MEDBUDDY_INTEGRATION=production` for live adapters (`real` / `live` are no longer accepted). Makefile targets are `be-dev-production` / `be-run-prod-production`; `render.yaml` and examples use `production`. Ruff/Black cleanups: import order in `models/domain.py`, `reminders/deliver.py` noqa for post-constant imports, unused imports removed in reminder tests, formatting in `channels/line/routes.py`.

- **Documentation (service role + multi-contact):** `docs/features.md`, `tdd-extended.md`, `tdd.md`, `use-cases.md`, `privacy.md`, `llm-context.md`, and `reminders.md` now describe `SUPABASE_SERVICE_KEY`, revoked `anon`/`authenticated` grants, the `emergency_contacts` table (replacing legacy `emergency_contact` text), updated `/v1/app/me` and onboarding JSON examples, ER diagrams, and LLM exclusion wording. `apps/backend/supabase/schema.sql` file header matches. `reminders.md` reminder test paths use `tests/medbuddy/reminders/`.

- **Post-add medication reply:** Assembly moved to `application/post_add_medication_reply.build_post_add_patient_reply`. Saves that only need one paragraph still use `compose_medication_added_reply`; when the patient already has other meds, the primary segment uses `compose_medication_added_primary` and list cross-check uses `post_add_interaction_crosscheck` (separate prompts from chat `interaction_check`). Extra purpose/CTA/boundary lines append only when compose falls back to `medication.added`.
- **Documentation:** `docs/features.md`, `use-cases.md`, `llm-context.md`, `privacy.md`, `tdd.md`, `tdd-extended.md`, `index.md`, `prd.md`, and `apps/backend/README.md` describe the post-add list cross-check (2+ medications), synthetic prompt keys, and privacy/context call sites.
- **LLM locale namespace:** Backend JSON and code now use **`llm.*`** instead of **`gemini.*`** for extraction, companion, and reminder-appendix strings (OpenAI, Gemini, mocks, tools, `reminders/prefs`) so prompts are not tied to one provider name.
- **Patient-facing copy (en / zh-TW):** Profile saves use full-sentence clauses and a clearer wrapper (“here’s what I changed”); emergency-contact fragments match that style. Simulated emergency notify lines stay explicit that no real SMS/call was sent but read in plain language. Medication add fallback, updates, add-confirm pending, and reminder-horizon nudges drop parenthetical “still waiting” tone. Vitals ack and drug-interaction pair lines are softened; interaction severities use conversational labels. Follow-up pass: medication list/upcoming/removal/reminder-disable copy, dose confirmations, LINE push wording, locale switch lines, health-summary headings, journal headers, softer LLM context (`prompts.*`, `llm.added_saved_facts`), and emergency saved-contact wording without “the note below.” **Micro-pass:** medication updates use `medication.updated` without a trailing “Notes:” line when empty, and `medication.updated_with_note` when instructions exist; add-confirm and list-hint copy are prose-first; “unspecified” dose/schedule reads as “Not filled in yet” / “還沒填”; vitals/profile acks and LINE `follow_welcome` drop stiff openers; placeholder detection treats those labels as unknown dose/timing tokens.
- **Documentation (patient copy / tools):** `docs/features.md` adds §4.5 `update_medication` and renumbers §4.6–§4.13; §8 references `confirm_dose` at §4.9. `docs/index.md` emergency quick lookup points to §4.11. `docs/use-cases.md` §3.1 / §3.3 / §3.12 and `docs/tdd-extended.md` orchestrator tool table document `medication.updated` vs `medication.updated_with_note`, list placeholder/hint behavior, and prose-first add-confirm. `apps/backend/README.md` localization notes the split update strings and placeholder labels.
- **TDD sync (standalone API docs):** Updated `docs/tdd-extended.md` to match current backend naming and responses (`require_mobile_auth`/`MobileAuthContext`, `generate_health_summary` tool name, `metadata` in `/v1/app/messages*` examples), and aligned the testing note with the layout under `tests/medbuddy/`.
- **Backend package layout:** `medbuddy.application` is grouped into subpackages **`pending/`** (early-turn resolvers + `locale_intents`), **`health_events/`** (`health_issue_events` policy + summary formatting), and **`profile/`** (profile patches, completion nudge, emergency-contact resolver); **`assistant_turn`**, **`patient_llm_context`**, and **`vital_log_build`** stay at the package root. Import paths under `medbuddy.application.*` changed accordingly.
- **Persistence (prior release note):** `vital_logs` → `health_issue_events`; greenfield installs use `apps/backend/supabase/schema.sql` (unified `routing_intent` / `created_at` columns).
- **Profile completion nudges:** When onboarding-style fields are still missing, the assistant reply from the main tool-orchestrator path may append a short footer every **`MEDBUDDY_PROFILE_COMPLETION_NUDGE_EVERY_N_USER_TURNS`** user messages (default **12**, **`0`** disables). Cadence is staggered per user via a stable hash so reminders stay occasional.
- **Emergency contact from chat:** Messages with a Taiwan mobile (`09xxxxxxxx`) plus clear family/emergency wording (or replying after the assistant asked for contact details) are saved via **`update_profile`** extraction **before** the medication tool loop — so misclassified lines like “my son, David, 0900111111” persist instead of being treated as a drug. Emergency intent replies now also suggest saving an emergency contact in chat once safe.
- **One-off medication reminders:** Quick asks like “a pill of aspirin in 1 minute” no longer stall on add-confirmation when extraction leaves dose unset — the backend applies a localized **1 pill** default for true one-off reminders (`first_reminder_in_minutes` + no daily materialization), expands dose cues (**pill** / **tablet**, minute-from-now phrases including Chinese **一分鐘後**), and the agent system prompt tells the model to confirm the reminder first instead of leading with “not on your list.”
- **Tool orchestrator context:** `run_tool_agent_loop` now prepends **redacted** prior **user/assistant** messages to `complete_chat_with_tools` (cap via **`MEDBUDDY_AGENT_ORCHESTRATOR_HISTORY_TURNS`**, default 12; `0` = none). System prompt nudges the model to use thread context for follow-ups. **MockLLM** step counter resets on the first hop of each turn when history makes `len(messages) > 2`.
- **Documentation sweep (branch-wide):** Aligned **`apps/backend/README.md`**, **`docs/tdd-extended.md`** (component map §3 + dispatch order §5 + config reference §15), **`docs/tdd.md`** (saved-contact branch hint), **`docs/use-cases.md`** (turn pipeline + §3.14 emergency), **`docs/features.md`** (§3.2 emergency-contact capture, §3.3 profile completion nudge, §4.11 emergency saved-contact branch), **`docs/privacy.md`** (subpackage paths + emergency-contact-from-chat exception row + summary cap), **`docs/index.md`** (new quick-lookup rows), and **`docs/go-port-mapping.md`** (application subpackages + orchestrator entry) with the new `application/{pending,health_events,profile}/` layout, the `emergency_contact_resolve` step, the saved-contact emergency branch (`agent.emergency_with_saved_contact` + metadata), and the `MEDBUDDY_PROFILE_COMPLETION_NUDGE_EVERY_N_USER_TURNS` post-reply footer.
- **Documentation sweep (same feature):** Aligned **privacy.md**, **prd.md**, **tdd.md**, **tdd-extended.md** (architecture + §5.1 + §8.3), **features.md** §2, **docs/index.md** quick lookup, presentation cost slide, and module docstring on **`orchestrator.py`** so orchestrator prior-thread behavior is described consistently.
- **Just-in-time medication understanding spec:** Added implementation-ready product specs for post-add/post-update medication-purpose cues, optional reminder refresher CTAs with cadence gates, bilingual microcopy templates (`en`, `zh-TW`), and rollout telemetry/guardrail thresholds in `docs/features.md` and `docs/use-cases.md`.
- **Just-in-time medication understanding runtime:** Implemented add/update/list educational cue flows in backend tools (purpose extraction from grounding + localized CTA/boundary copy), reminder-side refresher CTA injection with deterministic per-user+medication cadence (`MEDBUDDY_REMINDER_EDUCATION_CTA_EVERY_N_DAYS`, default 5), and tool metadata propagation for education telemetry (`education_cue_shown`, grounding/fallback flags, CTA-click proxy).
- **Docs + presentation**: Documentation and investor deck copy now describe only the **tool orchestrator** path (no intent→single-tool or legacy-dispatch framing). **Dead code removed:** `try_profile_intent_reply` (profile updates are **`update_profile` tool** + `apply_profile_update_from_extracted_patch` only).
- **Medication draft validation cleanup**: Extracted shared `medication_draft_fields_complete` logic for dose/schedule completeness (including one-time reminder schedule exception) and reused it in add-confirmation guardrails for clearer, consistent behavior.
- **Docs + deck (roadmap alignment)**: PRD/extended roadmap reflects prototype complete (Apr 2026), MVP + Taiwan pilot in flight (~Q3 exit target), Japan-first growth, MAU economics gates (< US$0.05 / < US$0.02), and mainland China as a separate JV/stack path; `features.md` T3.3 and investor *Problem → cost* slide updated (headline stats, citations, polypharmacy copy); `presentation` data-layer and reminder-queue failure-mode copy clarified; `apps/backend/README.md` aligned with post-refactor paths (`channels/api/`, `protocols/`, `services.py`, `core/`, `llm/prompts/`); `privacy.md` / `llm-context.md` tighten redaction vs de-identification wording; `tdd.md` / `tdd-extended.md` add Go/Fiber target-runtime framing + `go-port-mapping.md` links; `docs/index.md` and `go-port-mapping.md` navigation/markdown fixes; `use-cases.md` voice/onboarding paths; `TODO.md` checkoffs for completed checklist items.
- **Deck — glossary slide**: Added "Codes you'll see in this deck" slide (after "How to read this deck") covering OD-1–5, NG-1, T1/T2/T3, Ch 1–4, and 分包; visible in both VC and full modes.
- **Deck — language pass**: Removed "pivot-friendly" (×3 SVG labels) → "stage-gated"; removed "hypothesis the cohort can falsify" → "Every tier has an explicit unlock signal. We don't build until the signal fires."; removed "Nothing here is a commitment" → confident gate framing; reworded privacy slide "named, documented and audited, not slipped in by accident" → explicit scope/log/link phrasing; voice deferral rewritten as confident gate (OD-3) rather than apologetic hedge.
- **Deck — sources footer**: Universal brand footer (`MedBuddy · 2026`) added to all slides via CSS `::after`; added `.src[...]` citations to business model, infrastructure evolution, load forecast, polyglot persistence, and regional cloud slides; normalized existing citations.
- **Deck — income channels**: Merged "Income channels — references" and "Income channels — justification & guardrails" into one slide; references folded into `.src` footer.
- **Deck — roadmap**: Reframed slide 30 as sequencing philosophy ("signal → build → measure"); T1/T2/T3 tier overview stays as slide 31.
- **Deck — architecture captions**: Added "Same hexagonal core, two deployment shapes" caption above prototype and MVP/Growth architecture diagram pair.
- **Deck — Round-2 consistency pass**: Tightened architecture-diagram sublabels (`mobile · tests · integrations` → `API · tests · integrations`) in both Prototype and MVP/Growth diagrams; rewrote slide 9 to frame Expo as reference client (removed "MedBuddy mobile" product implication); realigned `prd.md` privacy wording with `privacy.md`'s redaction-vs-de-identification distinction; added inline glosses for OD-1 (regulatory class) on first roadmap-SVG body use and 分包 (unlabeled blister packs) on first T1 feature-card body use.
- **Deck — hyperlinks**: "How to read this deck" slide updated with doc-purpose table and dual PRD/TDD links (short + extended).
- **README**: "an optional Expo app" → "a reference Expo client for future mobile work"; standalone mobile app section retagged as "Reference / future / B2B2C surface".
- **prd.md**: Prototype Scope voice note updated to reference NG-1 / OD-3; explicit statement that voice metrics are not pilot pass/fail targets.
- **privacy.md**: Added "Redaction vs. de-identification" section clearly distinguishing current pattern-based masking (redaction) from planned NER-based clinical scrubbing (de-identification).
- **docs/index.md**: Added quick-lookup row pointing OD/T/Ch codes to `prd-extended.md` §13.

- **Go-port-ready backend refactor** (no behavioral changes): replaced Pydantic `BaseSettings` with a plain frozen `@dataclass` + explicit `load_settings(env)` function and `IntegrationMode`/`LlmProvider`/`Locale` enums; removed all silent `try/except ImportError` fallback paths in `container.py` (raises `ConfigError` instead); split `protocols/ports.py` into per-port files (`llm.py`, `user_data.py`, `line.py`, `speech.py`, `drugs.py`, `conversation.py`); moved `AppServices` from `engine/types.py` to `services.py`; regrouped cross-cutting utilities into `core/` (`errors.py`, `i18n.py`, `locale.py`, `timezone.py`, `logging.py`); renamed `channels/mobile/` → `channels/api/` and moved `http/shared_routes.py` → `channels/internal/routes.py`; folded `reminders/nudge_policy.py` into `reminders/prefs.py`; split `supabase_stores.py` (1 275 lines) into focused mixin files (`supabase_profile.py`, `supabase_medications.py`, `supabase_dose_events.py`, `supabase_conversations.py`); extracted shared LLM helpers to `integrations/llm/_common.py` and moved prompts to `llm/prompts/`; absorbed `drug_cache_keys.py` into `integrations/caching_drugs.py`; dropped `pydantic-settings` in favour of `python-dotenv`; all tests updated to use `make_mock_settings(**overrides)` helper.
- **Docs**: updated `tdd-extended.md` component map, config section, and flow diagrams to reflect new layout; added `docs/go-port-mapping.md` with a full Python → Go translation table.

### Added

- **Backend observability:** `build_patient_context_for_llm` logs the full assembled patient-context string at **INFO** (`medbuddy.llm.prompts.persona`) for debugging—may include preferred name and medication lines; revert or raise log level if too noisy or sensitive for your environment.
- **Patient context tests:** `tests/medbuddy/llm/prompts/test_persona_llm_safe.py` now asserts stored `preferred_name` appears in `build_patient_context_for_llm` (English and zh-TW), that the “preferred name not stored” gap is omitted when set, and whitespace-only names behave as missing; `format_patient_profile_signals_for_llm` docstring updated to match behavior.
- **Health issue event log (doctor summary):** Classifier-routed user turns matching a configurable allowlist (default: issue/wellness intents; exclude med-list/schedule CRUD; **`MEDBUDDY_HEALTH_ISSUE_LOG_INTENTS`**, with **`all_non_off_topic`**) are appended to **`health_issue_events`**. The doctor health summary and health-journal export include a **chronological formatted block** of these rows (cap **`MEDBUDDY_HEALTH_ISSUE_SUMMARY_EVENTS_LIMIT`**, default **60**), alongside recent chat. Structured vitals still use dedicated vital rows with **`routing_intent=log_vital`**.
- **LLM tool orchestrator**: Assistant turns use multi-step OpenAI tool calling (Gemini uses structured steps) with explicit tools for medications, reminders (including bulk clear/disable), health journal export, doctor summary, and **`simulate_notify_emergency_contact`** (simulated outreach — metadata for the app UI). **`POST /v1/app/messages`** returns optional **`metadata`** (e.g. `simulated_emergency_notification`).
- **`POST /v1/app/messages/voice`** (standalone HTTP): multipart audio upload → Speech-to-Text (profile **`locale`**) → same assistant turn as text → JSON **`reply`** + **`transcript`**. Expo uses **expo-speech** for playback on the client.
- **LINE voice replies**: After the assistant generates text, optional **Google Cloud Text-to-Speech** (MP3 → **m4a** via **ffmpeg**) plus ephemeral **`GET /v1/line/media/audio/{id}`** URLs let LINE receive **text + audio** in one reply batch. Toggle with **`MEDBUDDY_LINE_VOICE_REPLIES`** (`audio_inbound` default, `always`, or `off`). Requires **`PUBLIC_BASE_URL`** as **HTTPS** reachable by LINE, **`GOOGLE_SPEECH_PROJECT_ID`** (shared with STT / ADC), and **ffmpeg** on the server (included in the repo **Dockerfile**).
- **Upcoming dose schedule intent**: New `Intent.upcoming_doses` with `ListUpcomingDosesTool` answers “what’s next / today / soon” from materialized `dose_events` (not guessed frequency text). `UserDataPort.list_upcoming_dose_events` and shared window/formatting in `reminders/upcoming_display.py`. Patient context for external LLMs now includes the same authoritative schedule block via `application/patient_llm_context.patient_context_for_llm` (used by explain, interaction, side effects, health summary, fallback `compose_reply`, and post-add compose when reminders are already synced).
- **GitHub Actions CI**: Workflow runs backend Black + Ruff + pytest and frontend ESLint + TypeScript on pushes to `main` and on pull requests.
- **Emergency and side-effect intents**: Added `Intent.emergency` (fixed localized emergency reply, no LLM body generation) and `Intent.report_side_effects` with `ReportSideEffectsTool`.
- **Medication update + richer adherence flows**: Added `UpdateMedicationTool`, `ReportMissedDoseTool`, `LogVitalTool`, and side-effect-aware follow-up handling in conversation flows.
- **Medication-add confirmation state**: Added pending confirmation storage for incomplete medication drafts so users can confirm/cancel before save.
- **LINE reminders nudges**: Added optional follow-up nudges via `MEDBUDDY_REMINDER_NUDGE_INTERVALS_MINUTES`.
- **Multi-time daily reminder preferences**: Added structured reminder extraction and persistence for `daily_reminder_local_hhmm_list`.

### Changed

- **Investor deck (`docs/presentation/index.html`)**: *What it feels like* — **Beyond LINE — same assistant** (our app vs partner bullets); LINE column **`.feel-like-strip`** flex layout so three screenshots split the column evenly and scale taller; pilot LINE-first, mobile reference shots.
- **Investor deck (`docs/presentation/index.html`)**: *The adherence gap* — gradient donut + soft shadow, 50%/84% literacy comparison card, centred `gap-slide-wrap` layout; **Sources** footer uses `.src` (same style as Market sizing); wider comparison block; removed duplicate PMC cite under the card.
- **Investor deck (`docs/presentation/index.html`)**: Split *The adherence gap* and *Problem → cost* into two VC slides (compact deck ~17 slides); adherence / market / *Why now* / income-channel citations and copy in plainer VC language; package-insert wording avoids implying English-only leaflets for locally approved drugs; regulatory posture callout moved from *Why now* to PM *Who I serve*; removed adherence *Core insight* callout, *Patient Voice* line on ping slide, and *What MedBuddy is not* callout on *Three jobs*; Part 2 subtitle and English List example on *Three jobs*.
- **Investor deck (`docs/presentation/index.html`)**: **Removed** the VC slide *The ask — raising now to ship Taiwan, aim at Japan*; **reordered** Part 2 so *Conversation as the product* (*Part 2 — What MedBuddy is about*) precedes the wedge slide, then *Three jobs today*; **updated** founder/org-chart copy that referenced *The Ask*.
- **Investor deck (`docs/presentation/index.html`)**: Spell out languages instead of ISO-style codes (e.g. Traditional Chinese, English, Japanese, Korean, Thai, Indonesian) across wedge SVG, competitive and regional market tables, gates, MVP/pilot copy, and prototype appendix; **MedBuddy’s wedge** — Architectural Moat callout full-width under the two columns with the adapter-vs-product punch line on its own paragraph.
- **Investor deck (`docs/presentation/index.html`)**: “Why pings alone don’t work” — wider diagram (`viewBox` 800px, 100px gutters), stacked gap labels, two-line title + accent bar, patient-perspective lead (`.perspective-line`), updated feedback copy; **wedge** SVG text styling; **mode bar** and “How to read this deck” use **A** for full deck (replaces **F** / Shift+F) to avoid the browser **F** fullscreen shortcut; title slide footer and adherence-gap slide citations/rows tightened.
- **Investor deck (`docs/presentation/index.html`) — VC narrative audit**: Rebalanced coordination (caregiver/clinician) into the VC core; added “From conversation to coordination” slide; tightened wellness/regulatory, voice, OpenFDA, reminder SLO, PSP firewall, pilot cohort, persona, and “codebase readiness” copy; WHO adherence citation corroboration; ENG SLO cross-ref to Pilot G3.
- **Investor deck (`docs/presentation/index.html`)**: Clearer architecture subtitle and prototype-scope copy; insight blocks as HTML callouts (fixes remark `.cols[` parsing); more readable user-base and load-forecast slide; hide the remark `#source` textarea so raw slide metadata does not appear on screen; VC/ops slides use plain-language positioning (API paths and stack detail on **Prototype / ENG** appendix only); product roadmap closes with the same **diagram → callout** pattern as the phased roadmap.
- **Investor deck (`docs/presentation/index.html`)**: Stabilized GitHub Pages rendering by pinning slide bootstrapping to a vendored local `remark` script (with CDN fallback), forcing explicit slide-container viewport heights for flex/absolute title layouts, and adding a non-destructive boot error banner when `remark` fails to load.
- **Investor deck (`docs/presentation/index.html`)**: Follow-up GitHub Pages layout fix switches to responsive viewport meta (`device-width`), removes forced `100vh` slide-container overrides that conflicted with remark scaling, and moves title section lists from absolute positioning to normal flow to prevent overlap/clutter on the first slide.
- **Investor deck (`docs/presentation/index.html`)**: Firefox compatibility fix adds a full remark wrapper height chain (`.remark-container` through `.remark-slide`) plus `min-height: 100vh` on page root so `.remark-slide-content { height: 100%; }` resolves consistently and the deck fills the viewport like Chrome.
- **Investor deck (`docs/presentation/index.html`)**: Reverted the Firefox wrapper-height chain (which degraded Chrome scaling) and pinned `.remark-slide-content` to the deck's native 16:9 content height (`680px`) so remark scaling remains stable across browsers.
- **Investor deck — regional positioning**: Reframe wedge, “why now”, TAM/SAM funnel, and regulatory callout for Asia chat channels (LINE / Kakao / WhatsApp) and launch markets beyond Taiwan-only copy.
- **Investor deck (`docs/presentation/index.html`)**: *Income channels — mobile surfaces* — five mocks in one centered grid row (ladder/overlap removed); `safe center` vertical alignment; larger under-mock channel labels; removed the “Same assistant core…” footnote and the API mock’s “MedBuddy · tenant API” embed stripe.
- **Turn interpretation contract**: Replaced `classify_intent` with `interpret_user_turn` returning `TurnInterpretation` (`intent`, `record_pending_dose_as_taken`, `dose_adherence_note`), and wired `ConfirmDoseTool` to those structured fields.
- **Per-user locale behavior**: Standardized locale-aware responses across compose, interaction, medication-added responses, health summary labels, LINE welcome, and reminder pushes.
- **Integration/runtime wiring**: Improved container and app wiring (shared HTTP clients in production mode, safer logging defaults, stricter timezone validation).
- **Google Cloud Speech-to-Text**: Replaced REST + `GOOGLE_SPEECH_API_KEY` usage with the official `google-cloud-speech` v2 client (Application Default Credentials). `SpeechToTextPort.transcribe_m4a` accepts optional per-request `language_code`; LINE passes the user's effective locale for transcription.
- **Locale change detection**: Language-switch requests use structured `extract_locale_intent` first (fallback to profile patch), run early in `MedicationAgent`, and intent classification steers phrasing to `update_profile` where appropriate.
- **Supabase naming and schema alignment**: Consolidated profile storage around `patients`/`patient_id` and updated persistence adapters accordingly.
- **Integration package structure**: Reorganized backend adapters into `integrations/llm/`, `integrations/stt/`, and `integrations/persistence/`, and updated imports/tests/docs to match.

### Fixed

- **Profile completion nudge compatibility:** Profile-gap detection now treats legacy `emergency_contact` strings as valid fallback contact data when `emergency_contacts` is absent, preventing unnecessary profile-completion nudges for already-complete legacy rows.
- **Reminder horizon confirmation persistence:** `try_resolve_pending_reminder_horizon` now persists `raw_metadata.reminder` updates through `merge_medication_raw_metadata` (instead of unsupported `patch_medication({"raw_metadata": ...})`), clears pending horizon state only after a successful metadata write, and keeps pending on write failure to avoid false “confirmed” replies when `dose_events` were not actually materialized.
- **Dose confirmation detail + side-effect capture guidance**: Confirm-dose success replies now include the specific medication and dose when a single event is resolved, and intent-classification guidance/schema now explicitly captures dose-linked symptom notes across intents so adherence context is preserved for clinicians.
- **Dose confirmation targeting + note persistence**: Confirm-dose now auto-targets the most recently nudged pending dose within a short window, filters placeholder medication candidates from disambiguation, and preserves adherence taken/note slots detected during non-confirm intents via side-channel persistence.
- **One-time reminder extraction/confirmation flow**: Medication parsing now treats Chinese quantity words (e.g. `一顆`/`兩片`/`三粒`) as valid dosage evidence, clarifies dosage/schedule schema guidance for one-off reminders, and skips recurring-schedule confirmation requirements when only `first_reminder_in_minutes` is requested.
- **Dose-event reminder integrity and upcoming-list clarity**: Reconcile now marks stale unsent dose events as missed before re-enqueueing recent misses, first relative reminders are not re-materialized on every sync for medications with past events, and upcoming-dose replies suppress placeholder medication names while surfacing localized notices for incomplete names or pending reminder-horizon setup.
- **LINE delivery observability + integration-mode compatibility**: Added redacted inbound/LLM flow checkpoints and outbound LINE reply/push attempt/success/failure logs to speed webhook debugging, startup now logs the wired LINE client type, and settings parsing now supports legacy `MOCK_EXTERNAL_SERVICES` (including validation) when `MEDBUDDY_INTEGRATION` is unset.
- **Backend locale JSON stability**: Corrected invalid smart-quoted keys/strings in
  `apps/backend/src/medbuddy/locales/en.json` that could break JSON parsing and fail
  backend tests at startup/load time.
- **Medication add-confirm test alignment**: Updated backend test expectations so
  missing instructions alone no longer force add confirmation when dose/schedule
  are explicit, matching current confirmation policy.
- **Google STT (zh-TW)**: Traditional Chinese Taiwan is not supported as `zh-TW` with model `long` on location `global`. STT now sends `cmn-Hant-TW` with model `chirp` and uses a regional endpoint (default `asia-southeast1` when `GOOGLE_SPEECH_LOCATION` is `global`).
- **Medication dose/schedule copy in user locale**: List replies, reminders, confirm-dose options, add/update fallbacks, and incomplete-draft prompts now map stored English placeholders (e.g. `unspecified`) to the profile locale label (e.g. 未註明 for `zh-TW`). When the list still has missing dose or schedule, a short hint invites the user to send them for an update.
- **Google STT transcription**: Normalize language tags sent to Speech-to-Text v2 (short `en`/`zh` → `en-US`/`zh-TW`, underscore → hyphen) so locale-derived codes match API expectations.
- **Reminder materialization correctness**: Fixed multi-time-per-day expansion so `daily_local_hhmm_list` produces all expected `dose_events`.
- **Dose-note persistence**: Follow-up adherence notes now merge into recent taken dose rows when applicable.
- **Settings parsing**: Fixed startup parsing for comma-separated `MEDBUDDY_REMINDER_NUDGE_INTERVALS_MINUTES`.
- **LLM/output consistency**: Improved locale locking and corrected conflicting English prompt scaffolding.
- **Drug cache resolution**: Improved medication-name matching so personalization cache rows retain `medication_id` and grounding provenance more reliably.
- **LINE audio STT resilience**: Webhook audio handling now catches STT HTTP failures and sends a localized fallback reply instead of returning HTTP 500.
- **Emergency intent vs. saved emergency contact**: When `Intent.emergency` matches (fixed localized reply, no tool loop), users who already have **`emergency_contact`** now get the same **simulated** notify line as **`simulate_notify_emergency_contact`**, plus **`metadata.simulated_emergency_notification`** for the app banner; copy avoids asking them to add a contact “for next time.” Added **`agent.emergency_with_saved_contact`** (`en`, **`zh-TW`**) and API tests.

### Removed

- `LLMPort.extract_dose_confirmation_note` and related adapters/schemas after adherence was folded into `interpret_user_turn`.
- Obsolete duplicate assistant flow module `application/medication_intents.py`.

### Documentation

- **Presentation deck refresh:** Added architecture-style SVG visuals for conversation/reminder/privacy flows, converted tech stack to a compact table, expanded milestones visualization, and improved slide text contrast handling in `docs/presentation/index.html`.
- **Presentation deck spacing:** Tightened vertical spacing between the pipeline diagrams and the bullet text for the single conversation and reminders flow slides in `docs/presentation/index.html`.
- **Wedge visualization upgrade:** Replaced the static "triple intersection" wedge art with a D3-rendered diagram and refined layout styling (label chips, spacing, and center badge) for clearer readability in presentations.
- **TDD alignment:** Updated [`docs/tdd-extended.md`](docs/tdd-extended.md) component map (`integrations/{persistence,stt,tts}/…`, voice-related modules, `vital_logs` table, `patients.pending_agent_clarification`) and removed leftover "engineering exploratory" wording for LINE voice.
  - [`TODO.md`](TODO.md): documented **`POST /v1/app/messages/voice`** in the Expo integration summary.
  - **Reminder-trigger wording:** [`docs/features.md`](docs/features.md) §8 and [`docs/tdd-extended.md`](docs/tdd-extended.md) §3/§4.3 now list `update_medication` alongside add/remove as triggers for `sync_and_enqueue_reminders`.
- **PRD layout:** [`docs/prd.md`](docs/prd.md) is the **primary** condensed PRD; the full specification is [`docs/prd-extended.md`](docs/prd-extended.md). Removed `docs/prd-condensed.md` (merged into `prd.md`).
- **TDD layout:** [`docs/tdd.md`](docs/tdd.md) is the **primary** condensed TDD (~2 pages); the full design is [`docs/tdd-extended.md`](docs/tdd-extended.md) (former monolithic `tdd.md`).
- **README screenshots:** LINE and mobile concept images live under [`assets/screenshots/`](assets/screenshots/); root README shows multiple LINE samples and links to the feature catalog.
- Removed stale TTS and `internal-media` references from `apps/backend/README.md`,
  `docs/tdd.md`, `docs/tdd-extended.md`, `docs/prd.md`, `docs/features.md`, `docs/frontend-expo.md`,
  `docs/reminders.md`, `docs/use-cases.md`, and root `README.md`; Dockerfile install
  extras now match `pyproject.toml` (`[llm,supabase,reminders]`).

## [0.0.1] - 2026-04-05

### Added

- MedBuddy monorepo: FastAPI backend (`apps/backend`) with LINE webhook pipeline, mock/production
  integrations, and pytest suite.
- Expo (React Native) app (`apps/frontend`) with medication-focused UI, i18n (en, zh-TW), and
  ESLint + TypeScript checks.
- Container and Compose definitions (repo-root `Dockerfile`, `compose.yaml`).
- Repo automation: root `Makefile`, pre-commit (full backend lint/tests + frontend `npm run check`,
  require `CHANGELOG.md` staged with other changes), helper scripts under `scripts/`.
- Contributor UX: `.cursor/commands` (branch, commit, draft PR), `.cursor/rules` for backend and
  frontend standards, `.github/pull_request_template.md`, `.env.example` files, and `.gitignore`
  tuned for Python, Node, coverage, and local env files.
