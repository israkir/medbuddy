# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **Locale defaults:** LINE **follow** welcome and assistant replies seed `patients.locale` from LINE’s user profile **`language`** (with httpx `GET /v2/bot/profile/{userId}`) when it maps to `en` or `zh-TW`. Standalone **`GET /v1/app/me`** applies **`X-MedBuddy-Locale`** (device tag from Expo) or the **first** `Accept-Language` entry to update stored locale **only until** onboarding is completed, so server-side copy matches the device before the user submits onboarding.

### Fixed

- **Supabase schema:** `apps/backend/supabase/schema.sql` now includes explicit `GRANT` for `anon` / `authenticated` on MedBuddy tables (and `conversation_turns_id_seq`). Without these, PostgREST returns `42501 permission denied for table patients` when the backend uses `SUPABASE_PUBLISHABLE_KEY`.

### Changed

- **Profile save replies (emergency contact):** After saving, replies spell out the primary contact in plain language (relationship, name when present, channel, and stored value), e.g. “I’ve set your daughter Kathy’s phone (0912…) as your primary emergency contact,” with matching Traditional Chinese copy and correct handling of multiple contacts (primary row + count).
- **TDD sync (standalone API docs):** Updated `docs/tdd-extended.md` to match current backend naming and responses (`require_mobile_auth`/`MobileAuthContext`, `generate_health_summary` tool name, `metadata` in `/v1/app/messages*` examples), and corrected the testing note to reflect `tests/e2e/test_user_journeys.py`.
- **Backend package layout:** `medbuddy.application` is grouped into subpackages **`pending/`** (early-turn resolvers + `locale_intents`), **`health_events/`** (`health_issue_events` policy + summary formatting), and **`profile/`** (profile patches, completion nudge, emergency-contact resolver); **`assistant_turn`**, **`patient_llm_context`**, and **`vital_log_build`** stay at the package root. Import paths under `medbuddy.application.*` changed accordingly.
- **Persistence (prior release note):** `vital_logs` → `health_issue_events`; greenfield installs use `apps/backend/supabase/schema.sql` (unified `routing_intent` / `created_at` columns).
- **Profile completion nudges:** When onboarding-style fields are still missing, the assistant reply from the main tool-orchestrator path may append a short footer every **`MEDBUDDY_PROFILE_COMPLETION_NUDGE_EVERY_N_USER_TURNS`** user messages (default **12**, **`0`** disables). Cadence is staggered per user via a stable hash so reminders stay occasional.
- **Emergency contact from chat:** Messages with a Taiwan mobile (`09xxxxxxxx`) plus clear family/emergency wording (or replying after the assistant asked for contact details) are saved via **`update_profile`** extraction **before** the medication tool loop — so misclassified lines like “my son, David, 0900111111” persist instead of being treated as a drug. Emergency intent replies now also suggest saving an emergency contact in chat once safe.
- **One-off medication reminders:** Quick asks like “a pill of aspirin in 1 minute” no longer stall on add-confirmation when extraction leaves dose unset — the backend applies a localized **1 pill** default for true one-off reminders (`first_reminder_in_minutes` + no daily materialization), expands dose cues (**pill** / **tablet**, minute-from-now phrases including Chinese **一分鐘後**), and the agent system prompt tells the model to confirm the reminder first instead of leading with “not on your list.”
- **Tool orchestrator context:** `run_tool_agent_loop` now prepends **redacted** prior **user/assistant** messages to `complete_chat_with_tools` (cap via **`MEDBUDDY_AGENT_ORCHESTRATOR_HISTORY_TURNS`**, default 12; `0` = none). System prompt nudges the model to use thread context for follow-ups. **MockLLM** step counter resets on the first hop of each turn when history makes `len(messages) > 2`.
- **Documentation sweep (branch-wide):** Aligned **`apps/backend/README.md`**, **`docs/tdd-extended.md`** (component map §3 + dispatch order §5 + config reference §15), **`docs/tdd.md`** (saved-contact branch hint), **`docs/use-cases.md`** (turn pipeline + §3.14 emergency), **`docs/features.md`** (§3.2 emergency-contact capture, §3.3 profile completion nudge, §4.10 emergency saved-contact branch), **`docs/privacy.md`** (subpackage paths + emergency-contact-from-chat exception row + summary cap), **`docs/index.md`** (new quick-lookup rows), and **`docs/go-port-mapping.md`** (application subpackages + orchestrator entry) with the new `application/{pending,health_events,profile}/` layout, the `emergency_contact_resolve` step, the saved-contact emergency branch (`agent.emergency_with_saved_contact` + metadata), and the `MEDBUDDY_PROFILE_COMPLETION_NUDGE_EVERY_N_USER_TURNS` post-reply footer.
- **Documentation sweep (same feature):** Aligned **privacy.md**, **prd.md**, **tdd.md**, **tdd-extended.md** (architecture + §5.1 + §8.3), **features.md** §2, **docs/index.md** quick lookup, presentation cost slide, and module docstring on **`orchestrator.py`** so orchestrator prior-thread behavior is described consistently.
- **Just-in-time medication understanding spec:** Added implementation-ready product specs for post-add/post-update medication-purpose cues, optional reminder refresher CTAs with cadence gates, bilingual microcopy templates (`en`, `zh-TW`), and rollout telemetry/guardrail thresholds in `docs/features.md` and `docs/use-cases.md`.
- **Just-in-time medication understanding runtime:** Implemented add/update/list educational cue flows in backend tools (purpose extraction from grounding + localized CTA/boundary copy), reminder-side refresher CTA injection with deterministic per-user+medication cadence (`MEDBUDDY_REMINDER_EDUCATION_CTA_EVERY_N_DAYS`, default 5), and tool metadata propagation for education telemetry (`education_cue_shown`, grounding/fallback flags, CTA-click proxy).
- **Docs + presentation**: Documentation and investor deck copy now describe only the **tool orchestrator** path (no intent→single-tool or legacy-dispatch framing). **Dead code removed:** `try_profile_intent_reply` (profile updates are **`update_profile` tool** + `apply_profile_update_from_extracted_patch` only).
- **Medication draft validation cleanup**: Extracted shared `medication_draft_fields_complete` logic for dose/schedule completeness (including one-time reminder schedule exception) and reused it in add-confirmation guardrails for clearer, consistent behavior.
- **Docs + deck (roadmap alignment)**: PRD/extended roadmap reflects prototype complete (Apr 2026), MVP + Taiwan pilot in flight (~Q3 exit target), Japan-first growth, MAU economics gates (< US$0.05 / < US$0.02), and mainland China as a separate JV/stack path; `features.md` T3.3 and investor *Problem → cost* slide updated (headline stats, citations, polypharmacy copy); `presentation` data-layer and reminder-queue failure-mode copy clarified; `apps/backend/README.md` aligned with post-refactor paths (`channels/api/`, `protocols/`, `services.py`, `core/`, `llm/prompts/`); `privacy.md` / `llm-context.md` tighten redaction vs de-identification wording; `tdd.md` / `tdd-extended.md` add Go/Fiber target-runtime framing + `go-port-mapping.md` links; `docs/index.md` and `go-port-mapping.md` navigation/markdown fixes; `use-cases.md` voice/onboarding paths; `TODO.md` checkoffs for completed checklist items.
- **Deck — glossary slide**: Added "Codes you'll see in this deck" slide (after "How to read this deck") covering OD-1–5, NG-1, T1/T2/T3, Ch 1–4, and 分包; visible in both VC and full modes.
- **Deck — language pass**: Removed "pivot-friendly" (×3 SVG labels) → "stage-gated"; removed "hypothesis the cohort can falsify" → "Every tier has an explicit unlock signal. We don't build until the signal fires."; removed "Nothing here is a commitment" → confident gate framing; reworded privacy slide "named, documented and audited, not slipped in by accident" → explicit scope/log/link phrasing; voice deferral rewritten as confident gate (OD-3) rather than apologetic hedge.
- **Deck — sources footer**: Universal brand footer (`MedBuddy · 2026`) added to all slides via CSS `::after`; added `.src[...]` citations to business model, infrastructure evolution, load forecast, polyglot persistence, and regional cloud slides; normalized existing citations.
- **Deck — income channels**: Merged "Income channels — references" and "Income channels — justification & guardrails" into one slide; references folded into `.src` footer.
- **Deck — roadmap**: Reframed slide 30 as sequencing philosophy ("signal → build → measure"); T1/T2/T3 tier overview stays as slide 31.
- **Deck — architecture captions**: Added "Same hexagonal core, two deployment shapes" caption above prototype and MVP/Growth architecture diagram pair.
- **Deck — Round-2 consistency pass**: Tightened architecture-diagram sublabels (`mobile · tests · integrations` → `API · tests · integrations`) in both Prototype and MVP/Growth diagrams; rewrote slide 9 to frame Expo as reference client (removed "MedBuddy mobile" product implication); realigned `prd.md` privacy wording with `privacy.md`'s redaction-vs-de-identification distinction; added inline glosses for OD-1 (regulatory class) on first roadmap-SVG body use and 分包 (unlabeled blister packs) on first T1 feature-card body use.
- **Deck — hyperlinks**: "How to read this deck" slide updated with doc-purpose table and dual PRD/TDD links (short + extended).
- **README**: "an optional Expo app" → "a reference Expo client for future mobile work"; standalone mobile app section retagged as "Reference / future / B2B2C surface".
- **prd.md**: Prototype Scope voice note updated to reference NG-1 / OD-3; explicit statement that voice metrics are not pilot pass/fail targets.
- **privacy.md**: Added "Redaction vs. de-identification" section clearly distinguishing current pattern-based masking (redaction) from planned NER-based clinical scrubbing (de-identification).
- **docs/index.md**: Added quick-lookup row pointing OD/T/Ch codes to `prd-extended.md` §13.

- **Go-port-ready backend refactor** (no behavioral changes): replaced Pydantic `BaseSettings` with a plain frozen `@dataclass` + explicit `load_settings(env)` function and `IntegrationMode`/`LlmProvider`/`Locale` enums; removed all silent `try/except ImportError` fallback paths in `container.py` (raises `ConfigError` instead); split `protocols/ports.py` into per-port files (`llm.py`, `user_data.py`, `line.py`, `speech.py`, `drugs.py`, `conversation.py`); moved `AppServices` from `engine/types.py` to `services.py`; regrouped cross-cutting utilities into `core/` (`errors.py`, `i18n.py`, `locale.py`, `timezone.py`, `logging.py`); renamed `channels/mobile/` → `channels/api/` and moved `http/shared_routes.py` → `channels/internal/routes.py`; folded `reminders/nudge_policy.py` into `reminders/prefs.py`; split `supabase_stores.py` (1 275 lines) into focused mixin files (`supabase_profile.py`, `supabase_medications.py`, `supabase_dose_events.py`, `supabase_conversations.py`); extracted shared LLM helpers to `integrations/llm/_common.py` and moved prompts to `llm/prompts/`; absorbed `drug_cache_keys.py` into `integrations/caching_drugs.py`; dropped `pydantic-settings` in favour of `python-dotenv`; all tests updated to use `make_mock_settings(**overrides)` helper.
- **Docs**: updated `tdd-extended.md` component map, config section, and flow diagrams to reflect new layout; added `docs/go-port-mapping.md` with a full Python → Go translation table.

### Added

- **Backend observability:** `build_patient_context_for_llm` logs the full assembled patient-context string at **INFO** (`medbuddy.llm.prompts.persona`) for debugging—may include preferred name and medication lines; revert or raise log level if too noisy or sensitive for your environment.
- **Patient context tests:** `tests/prompts/test_persona_llm_safe.py` now asserts stored `preferred_name` appears in `build_patient_context_for_llm` (English and zh-TW), that the “preferred name not stored” gap is omitted when set, and whitespace-only names behave as missing; `format_patient_profile_signals_for_llm` docstring updated to match behavior.
- **Health issue event log (doctor summary):** Classifier-routed user turns matching a configurable allowlist (default: issue/wellness intents; exclude med-list/schedule CRUD; **`MEDBUDDY_HEALTH_ISSUE_LOG_INTENTS`**, with **`all_non_off_topic`**) are appended to **`health_issue_events`**. The doctor health summary and health-journal export include a **chronological formatted block** of these rows (cap **`MEDBUDDY_HEALTH_ISSUE_SUMMARY_EVENTS_LIMIT`**, default **60**), alongside recent chat. Structured vitals still use dedicated vital rows with **`routing_intent=log_vital`**.
- **LLM tool orchestrator**: Assistant turns use multi-step OpenAI tool calling (Gemini uses structured steps) with explicit tools for medications, reminders (including bulk clear/disable), health journal export, doctor summary, and **`simulate_notify_emergency_contact`** (simulated outreach — metadata for the app UI). **`POST /v1/app/messages`** returns optional **`metadata`** (e.g. `simulated_emergency_notification`).
- **`POST /v1/app/messages/voice`** (standalone HTTP): multipart audio upload → Speech-to-Text (profile **`locale`**) → same assistant turn as text → JSON **`reply`** + **`transcript`**. Expo uses **expo-speech** for playback on the client.
- **LINE voice replies**: After the assistant generates text, optional **Google Cloud Text-to-Speech** (MP3 → **m4a** via **ffmpeg**) plus ephemeral **`GET /v1/line/media/audio/{id}`** URLs let LINE receive **text + audio** in one reply batch. Toggle with **`MEDBUDDY_LINE_VOICE_REPLIES`** (`audio_inbound` default, `always`, or `off`). Requires **`PUBLIC_BASE_URL`** as **HTTPS** reachable by LINE, **`GOOGLE_SPEECH_PROJECT_ID`** (shared with STT / ADC), and **ffmpeg** on the server (included in the repo **Dockerfile**).
- **Upcoming dose schedule intent**: New `Intent.upcoming_doses` with `ListUpcomingDosesTool` answers “what’s next / today / soon” from materialized `dose_events` (not guessed frequency text). `UserDataPort.list_upcoming_dose_events` and shared window/formatting in `reminders/upcoming_display.py`. Patient context for external LLMs now includes the same authoritative schedule block via `application/patient_llm_context.patient_context_for_llm` (used by explain, interaction, side effects, health summary, fallback `compose_reply`, and post-add compose when reminders are already synced).
- **GitHub Actions CI**: Workflow runs backend Black + Ruff + pytest and frontend ESLint + TypeScript on pushes to `main` and on pull requests.
- **Emergency and side-effect intents**: Added `Intent.emergency` (fixed localized emergency reply, no LLM body generation) and `Intent.report_side_effects` with `ReportSideEffectsTool`.
- **Medication update + richer adherence flows**: Added `UpdateMedicationTool`, `ReportMissedDoseTool`, `LogVitalTool`, and side-effect-aware follow-up handling in conversation flows.
- **Medication-add confirmation state**: Added pending confirmation storage for incomplete medication drafts so users can confirm/cancel before save.
- **LINE reminders nudges**: Added optional follow-up nudges via `MEDBUDDY_REMINDER_NUDGE_INTERVALS_MINUTES`.
- **Multi-time daily reminder preferences**: Added structured reminder extraction and persistence for `daily_reminder_local_hhmm_list`.

### Changed

- **Investor deck (`docs/presentation/index.html`)**: *What it feels like* — **Beyond LINE — same assistant** (our app vs partner bullets); LINE column **`.feel-like-strip`** flex layout so three screenshots split the column evenly and scale taller; pilot LINE-first, mobile reference shots.
- **Investor deck (`docs/presentation/index.html`)**: *The adherence gap* — gradient donut + soft shadow, 50%/84% literacy comparison card, centred `gap-slide-wrap` layout; **Sources** footer uses `.src` (same style as Market sizing); wider comparison block; removed duplicate PMC cite under the card.
- **Investor deck (`docs/presentation/index.html`)**: Split *The adherence gap* and *Problem → cost* into two VC slides (compact deck ~17 slides); adherence / market / *Why now* / income-channel citations and copy in plainer VC language; package-insert wording avoids implying English-only leaflets for locally approved drugs; regulatory posture callout moved from *Why now* to PM *Who I serve*; removed adherence *Core insight* callout, *Patient Voice* line on ping slide, and *What MedBuddy is not* callout on *Three jobs*; Part 2 subtitle and English List example on *Three jobs*.
- **Investor deck (`docs/presentation/index.html`)**: **Removed** the VC slide *The ask — raising now to ship Taiwan, aim at Japan*; **reordered** Part 2 so *Conversation as the product* (*Part 2 — What MedBuddy is about*) precedes the wedge slide, then *Three jobs today*; **updated** founder/org-chart copy that referenced *The Ask*.
- **Investor deck (`docs/presentation/index.html`)**: Spell out languages instead of ISO-style codes (e.g. Traditional Chinese, English, Japanese, Korean, Thai, Indonesian) across wedge SVG, competitive and regional market tables, gates, MVP/pilot copy, and prototype appendix; **MedBuddy’s wedge** — Architectural Moat callout full-width under the two columns with the adapter-vs-product punch line on its own paragraph.
- **Investor deck (`docs/presentation/index.html`)**: “Why pings alone don’t work” — wider diagram (`viewBox` 800px, 100px gutters), stacked gap labels, two-line title + accent bar, patient-perspective lead (`.perspective-line`), updated feedback copy; **wedge** SVG text styling; **mode bar** and “How to read this deck” use **A** for full deck (replaces **F** / Shift+F) to avoid the browser **F** fullscreen shortcut; title slide footer and adherence-gap slide citations/rows tightened.
- **Investor deck (`docs/presentation/index.html`) — VC narrative audit**: Rebalanced coordination (caregiver/clinician) into the VC core; added “From conversation to coordination” slide; tightened wellness/regulatory, voice, OpenFDA, reminder SLO, PSP firewall, pilot cohort, persona, and “codebase readiness” copy; WHO adherence citation corroboration; ENG SLO cross-ref to Pilot G3.
- **Investor deck (`docs/presentation/index.html`)**: Clearer architecture subtitle and prototype-scope copy; insight blocks as HTML callouts (fixes remark `.cols[` parsing); more readable user-base and load-forecast slide; hide the remark `#source` textarea so raw slide metadata does not appear on screen; VC/ops slides use plain-language positioning (API paths and stack detail on **Prototype / ENG** appendix only); product roadmap closes with the same **diagram → callout** pattern as the phased roadmap.
- **Investor deck (`docs/presentation/index.html`)**: Stabilized GitHub Pages rendering by pinning slide bootstrapping to a vendored local `remark` script (with CDN fallback), forcing explicit slide-container viewport heights for flex/absolute title layouts, and adding a non-destructive boot error banner when `remark` fails to load.
- **Investor deck (`docs/presentation/index.html`)**: Follow-up GitHub Pages layout fix switches to responsive viewport meta (`device-width`), removes forced `100vh` slide-container overrides that conflicted with remark scaling, and moves title section lists from absolute positioning to normal flow to prevent overlap/clutter on the first slide.
- **Investor deck (`docs/presentation/index.html`)**: Firefox compatibility fix adds a full remark wrapper height chain (`.remark-container` through `.remark-slide`) plus `min-height: 100vh` on page root so `.remark-slide-content { height: 100%; }` resolves consistently and the deck fills the viewport like Chrome.
- **Investor deck (`docs/presentation/index.html`)**: Reverted the Firefox wrapper-height chain (which degraded Chrome scaling) and pinned `.remark-slide-content` to the deck's native 16:9 content height (`680px`) so remark scaling remains stable across browsers.
- **Investor deck — regional positioning**: Reframe wedge, “why now”, TAM/SAM funnel, and regulatory callout for Asia chat channels (LINE / Kakao / WhatsApp) and launch markets beyond Taiwan-only copy.
- **Investor deck (`docs/presentation/index.html`)**: *Income channels — mobile surfaces* — five mocks in one centered grid row (ladder/overlap removed); `safe center` vertical alignment; larger under-mock channel labels; removed the “Same assistant core…” footnote and the API mock’s “MedBuddy · tenant API” embed stripe.
- **Turn interpretation contract**: Replaced `classify_intent` with `interpret_user_turn` returning `TurnInterpretation` (`intent`, `record_pending_dose_as_taken`, `dose_adherence_note`), and wired `ConfirmDoseTool` to those structured fields.
- **Per-user locale behavior**: Standardized locale-aware responses across compose, interaction, medication-added responses, health summary labels, LINE welcome, and reminder pushes.
- **Integration/runtime wiring**: Improved container and app wiring (shared HTTP clients in real mode, safer logging defaults, stricter timezone validation).
- **Google Cloud Speech-to-Text**: Replaced REST + `GOOGLE_SPEECH_API_KEY` usage with the official `google-cloud-speech` v2 client (Application Default Credentials). `SpeechToTextPort.transcribe_m4a` accepts optional per-request `language_code`; LINE passes the user's effective locale for transcription.
- **Locale change detection**: Language-switch requests use structured `extract_locale_intent` first (fallback to profile patch), run early in `MedicationAgent`, and intent classification steers phrasing to `update_profile` where appropriate.
- **Supabase naming and schema alignment**: Consolidated profile storage around `patients`/`patient_id` and updated persistence adapters accordingly.
- **Integration package structure**: Reorganized backend adapters into `integrations/llm/`, `integrations/stt/`, and `integrations/persistence/`, and updated imports/tests/docs to match.

### Fixed

- **Profile completion nudge compatibility:** Profile-gap detection now treats legacy `emergency_contact` strings as valid fallback contact data when `emergency_contacts` is absent, preventing unnecessary profile-completion nudges for already-complete legacy rows.
- **Reminder horizon confirmation persistence:** `try_resolve_pending_reminder_horizon` now persists `raw_metadata.reminder` updates through `merge_medication_raw_metadata` (instead of unsupported `patch_medication({"raw_metadata": ...})`), clears pending horizon state only after a successful metadata write, and keeps pending on write failure to avoid false “confirmed” replies when `dose_events` were not actually materialized.
- **Dose confirmation detail + side-effect capture guidance**: Confirm-dose success replies now include the specific medication and dose when a single event is resolved, and intent-classification guidance/schema now explicitly captures dose-linked symptom notes across intents so adherence context is preserved for clinicians.
- **Dose confirmation targeting + note persistence**: Confirm-dose now auto-targets the most recently nudged pending dose within a short window, filters placeholder medication candidates from disambiguation, and preserves adherence taken/note slots detected during non-confirm intents via side-channel persistence.
- **One-time reminder extraction/confirmation flow**: Medication parsing now treats Chinese quantity words (e.g. `一顆`/`兩片`/`三粒`) as valid dosage evidence, clarifies dosage/schedule schema guidance for one-off reminders, and skips recurring-schedule confirmation requirements when only `first_reminder_in_minutes` is requested.
- **Dose-event reminder integrity and upcoming-list clarity**: Reconcile now marks stale unsent dose events as missed before re-enqueueing recent misses, first relative reminders are not re-materialized on every sync for medications with past events, and upcoming-dose replies suppress placeholder medication names while surfacing localized notices for incomplete names or pending reminder-horizon setup.
- **LINE delivery observability + integration-mode compatibility**: Added redacted inbound/LLM flow checkpoints and outbound LINE reply/push attempt/success/failure logs to speed webhook debugging, startup now logs the wired LINE client type, and settings parsing now supports legacy `MOCK_EXTERNAL_SERVICES` (including validation) when `MEDBUDDY_INTEGRATION` is unset.
- **Backend locale JSON stability**: Corrected invalid smart-quoted keys/strings in
  `apps/backend/src/medbuddy/locales/en.json` that could break JSON parsing and fail
  backend tests at startup/load time.
- **Medication add-confirm test alignment**: Updated backend test expectations so
  missing instructions alone no longer force add confirmation when dose/schedule
  are explicit, matching current confirmation policy.
- **Google STT (zh-TW)**: Traditional Chinese Taiwan is not supported as `zh-TW` with model `long` on location `global`. STT now sends `cmn-Hant-TW` with model `chirp` and uses a regional endpoint (default `asia-southeast1` when `GOOGLE_SPEECH_LOCATION` is `global`).
- **Medication dose/schedule copy in user locale**: List replies, reminders, confirm-dose options, add/update fallbacks, and incomplete-draft prompts now map stored English placeholders (e.g. `unspecified`) to the profile locale label (e.g. 未註明 for `zh-TW`). When the list still has missing dose or schedule, a short hint invites the user to send them for an update.
- **Google STT transcription**: Normalize language tags sent to Speech-to-Text v2 (short `en`/`zh` → `en-US`/`zh-TW`, underscore → hyphen) so locale-derived codes match API expectations.
- **Reminder materialization correctness**: Fixed multi-time-per-day expansion so `daily_local_hhmm_list` produces all expected `dose_events`.
- **Dose-note persistence**: Follow-up adherence notes now merge into recent taken dose rows when applicable.
- **Settings parsing**: Fixed startup parsing for comma-separated `MEDBUDDY_REMINDER_NUDGE_INTERVALS_MINUTES`.
- **LLM/output consistency**: Improved locale locking and corrected conflicting English prompt scaffolding.
- **Drug cache resolution**: Improved medication-name matching so personalization cache rows retain `medication_id` and grounding provenance more reliably.
- **LINE audio STT resilience**: Webhook audio handling now catches STT HTTP failures and sends a localized fallback reply instead of returning HTTP 500.
- **Emergency intent vs. saved emergency contact**: When `Intent.emergency` matches (fixed localized reply, no tool loop), users who already have **`emergency_contact`** now get the same **simulated** notify line as **`simulate_notify_emergency_contact`**, plus **`metadata.simulated_emergency_notification`** for the app banner; copy avoids asking them to add a contact “for next time.” Added **`agent.emergency_with_saved_contact`** (`en`, **`zh-TW`**) and API tests.

### Removed

- `LLMPort.extract_dose_confirmation_note` and related adapters/schemas after adherence was folded into `interpret_user_turn`.
- Obsolete duplicate assistant flow module `application/medication_intents.py`.

### Documentation

- **Presentation deck refresh:** Added architecture-style SVG visuals for conversation/reminder/privacy flows, converted tech stack to a compact table, expanded milestones visualization, and improved slide text contrast handling in `docs/presentation/index.html`.
- **Wedge visualization upgrade:** Replaced the static "triple intersection" wedge art with a D3-rendered diagram and refined layout styling (label chips, spacing, and center badge) for clearer readability in presentations.
- **TDD alignment:** Updated [`docs/tdd-extended.md`](docs/tdd-extended.md) component map (`integrations/{persistence,stt,tts}/…`, voice-related modules, `vital_logs` table, `patients.pending_agent_clarification`) and removed leftover "engineering exploratory" wording for LINE voice.
  - [`TODO.md`](TODO.md): documented **`POST /v1/app/messages/voice`** in the Expo integration summary.
  - **Reminder-trigger wording:** [`docs/features.md`](docs/features.md) §8 and [`docs/tdd-extended.md`](docs/tdd-extended.md) §3/§4.3 now list `update_medication` alongside add/remove as triggers for `sync_and_enqueue_reminders`.
- **PRD layout:** [`docs/prd.md`](docs/prd.md) is the **primary** condensed PRD; the full specification is [`docs/prd-extended.md`](docs/prd-extended.md). Removed `docs/prd-condensed.md` (merged into `prd.md`).
- **TDD layout:** [`docs/tdd.md`](docs/tdd.md) is the **primary** condensed TDD (~2 pages); the full design is [`docs/tdd-extended.md`](docs/tdd-extended.md) (former monolithic `tdd.md`).
- **README screenshots:** LINE and mobile concept images live under [`assets/screenshots/`](assets/screenshots/); root README shows multiple LINE samples and links to the feature catalog.
- Removed stale TTS and `internal-media` references from `apps/backend/README.md`,
  `docs/tdd.md`, `docs/tdd-extended.md`, `docs/prd.md`, `docs/features.md`, `docs/frontend-expo.md`,
  `docs/reminders.md`, `docs/use-cases.md`, and root `README.md`; Dockerfile install
  extras now match `pyproject.toml` (`[llm,supabase,reminders]`).

## [0.0.1] - 2026-04-05

### Added

- MedBuddy monorepo: FastAPI backend (`apps/backend`) with LINE webhook pipeline, mock/real
  integrations, and pytest suite.
- Expo (React Native) app (`apps/frontend`) with medication-focused UI, i18n (en, zh-TW), and
  ESLint + TypeScript checks.
- Container and Compose definitions (repo-root `Dockerfile`, `compose.yaml`).
- Repo automation: root `Makefile`, pre-commit (full backend lint/tests + frontend `npm run check`,
  require `CHANGELOG.md` staged with other changes), helper scripts under `scripts/`.
- Contributor UX: `.cursor/commands` (branch, commit, draft PR), `.cursor/rules` for backend and
  frontend standards, `.github/pull_request_template.md`, `.env.example` files, and `.gitignore`
  tuned for Python, Node, coverage, and local env files.
