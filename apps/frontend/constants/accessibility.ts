/** Minimum touch target ~48dp (Material) / 44pt (Apple HIG) for elderly users. */
export const MIN_TOUCH = 48;

export const fontSize = {
  /** Body text — larger than default for low vision / aging eyes */
  body: 22,
  title: 28,
  caption: 18,
  tabLabel: 11,
} as const;
